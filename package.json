{
  "name": "king",
  "private": true,
  "version": "0.1.0",
  "description": "KING — jogo de cartas multiplayer (web + app). Monorepo. Família Verbete.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace apps/web",
    "test": "npm run test --workspace packages/engine && npm run test --workspace apps/web",
    "build": "npm run build --workspace packages/engine && npm run build --workspace apps/web"
  },
  "engines": {
    "node": ">=18"
  }
}
