node_modules/
dist/
build/
*.log
.DS_Store
.env
.env.local
coverage/
# Playwright (relatórios e artefatos de diagnóstico)
playwright-report/
test-results/
blob-report/
playwright/.cache/
# Vercel (link local: contém IDs de projeto/conta — não versionar)
.vercel/
# mobile (Capacitor) — geradas depois
android/
ios/

# Binários gerados (APK/AAB e afins). Nunca vão para o repositório.
artifacts/
